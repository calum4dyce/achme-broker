import { PolicyTypePipe } from './policy-type.pipe';

describe('PolicyTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PolicyTypePipe();
    expect(pipe).toBeTruthy();
  });
});
